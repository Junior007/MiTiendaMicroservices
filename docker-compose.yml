version: '3.4'

services:
  catalogdb:
    image: mongo

  basketdb:
    image: redis

  catalog.api:
    container_name: catalogapi

  basket.api:
    container_name: basketapi
 
    
  mongo-express:
    image: mongo-express